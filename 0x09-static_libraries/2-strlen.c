#include "main.h"
/**
 * _strlen - a function that returns the length of a string
 * @s: the string that is wanted to know it's length
 *
 * Return: returns the length of a string
 */

int _strlen(char *s)
{
	return (strlen(s));
}
